# Copy to secret.env and set real values before deploying. secret.env
# is gitignored — never commit it. For a longer-lived setup, replace
# this flow with External Secrets Operator + AWS Secrets Manager.
POSTGRES_USER=app
POSTGRES_PASSWORD=change-me
POSTGRES_DB=tasks
